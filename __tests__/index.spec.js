describe("test", () => {
  it("should pass", () => {
    expect(true).toBe(false);
  });
  it("should pass", () => {
    expect(true).toBe(false);
  });
});

test("test", () => {
  expect(1).toBe(0);
});
